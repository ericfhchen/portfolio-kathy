import { imageProjects } from './imageProject'
import { videoProjects } from './videoProject'
import { clientType } from './clients'

export const schema = {
  types: [imageProjects, videoProjects, clientType],
}
